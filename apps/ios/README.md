# iOS client

Reserved composition root for a future iOS client. It will consume the versioned gateway contract and own iOS lifecycle, pairing, Keychain integration, audio, notifications, and packaging—not Quad Cortex protocol logic.

The UI framework remains deliberately undecided.
