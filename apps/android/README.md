# Android client

Reserved composition root for a future Android client. It will consume the versioned gateway contract and own Android lifecycle, pairing, credentials, audio, notifications, and packaging—not Quad Cortex protocol logic.

The UI framework remains deliberately undecided.
