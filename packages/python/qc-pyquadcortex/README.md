# qc-pyquadcortex

Adapter implementing the `qc-core` device port with `pyquadcortex`. It owns raw HID/protobuf translation and firmware compatibility quirks. No app or service may use `pyquadcortex` except through this adapter.
