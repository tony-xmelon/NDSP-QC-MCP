# qc-core

Pure Python domain and application layer: normalized models, use cases, ports, safety rules, expected-state validation, connection states, and command-journal interfaces.

Forbidden dependencies include HID, `pyquadcortex`, protobuf wire models, MCP, HTTP frameworks, UI frameworks, and operating-system APIs.
