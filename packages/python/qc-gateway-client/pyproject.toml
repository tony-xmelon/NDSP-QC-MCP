[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ndsp-qc-gateway-client"
version = "0.1.0"
description = "Typed Python client for the NDSP QC Gateway v1 contract"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/qc_gateway_client"]
