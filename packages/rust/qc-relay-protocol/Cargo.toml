[package]
name = "qc-relay-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared wire frames for QC Control public-relay device clients"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

