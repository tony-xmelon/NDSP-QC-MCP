# Cross-module tests

This directory hosts contract compatibility, gateway/MCP integration, fault-injection, packaging, and opt-in real-hardware smoke tests. Unit tests remain beside their owning modules.
